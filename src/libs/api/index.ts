// Frontend APIs (내부 Next.js API routes, apiClient 사용)
export * from './frontend';

// Backend APIs (백엔드 서버, backendClient 사용)
export * from './backend';

// External APIs (외부 서비스, externalClient 사용)
export * from './external';
